<%= form_with(model: [@promo, @classroom], local: true) do |form| %>
  <% if classroom.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(classroom.errors.count, "error") %> prohibited this classroom from being saved:</h2>

      <ul>
        <% classroom.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :"Crée la Classe:" %>
    <%= form.text_field :title , placeholder: "Nom de la Classe"  %>
  </div>


  <div class="actions">
    <%= form.submit "Ajoute la Classe", class: 'btn btn-primary' %>
    <%= link_to 'Annuler', @promo, class: 'btn btn-secondary' %>
  </div>
<% end %>
